/**
 * Estamos exportando search pero con el Alias searchView
 * al poner un alias cuando importamos esta vista usaremos ese alias
 */
export {default as SearchView} from "./Search";

export {default as UsersView} from "./Users";

export { default as UserView } from "./User";